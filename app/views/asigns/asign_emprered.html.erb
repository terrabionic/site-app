<p id="notice"><%= notice %></p>

<div class="layout">

	<%= render 'shared/nav' %>
	
	<div class="main">



		<%= link_to 'Atrás', companies_path, class: "back-button" %>

		<h1 class="main-headline inline-block">Agentes disponibles</h1>

		<h2 class="color-green text-bigger mb-phi">Emprered</h2>

		<table class="site-table">
			<thead>
				<tr>
				  <th>Nombre</th>
				  <th></th>
				  <th></th>
				</tr>
			</thead>
			<tbody>
				<% if @emprereds %>
					<% @emprereds.each do |emprered| %>
						<tr>
						  <td><%= emprered.name %></td>
						  <td>
							<% if emprered.available %>
								<strong>Disponible</strong>
							<% else %>
								<strong>Ocupado</strong>
							<% end %>
						  </td>
						  <td>
							<% if emprered.available %>
								<%=link_to "Asignar", asignar_emprered_path(user_id: emprered, company_id: @company ), method: :post %>	
							<% end %>
						  </td>
						</tr>
					<% end %>
				<% end %>
			</tbody>
		</table>
		
	</div><!-- .main -->
</div><!-- .layout -->

<%= render 'shared/footer' %>