<%= form_for(@login_manager) do |f| %>
  <% if @login_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login_manager.errors.count, "error") %> prohibited this login_manager from being saved:</h2>

      <ul>
      <% @login_manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Email de contacto</strong><br>
    <%= f.text_field :email_contact %>
  </div>
  <div class="field">
    <strong>Quienes somos</strong><br>
    <%= f.text_area :about_us %>
  </div>
  <div class="field">
    <strong>Aviso de privacidad</strong><br>
    <%= f.text_area :privacy %>
  </div>
  <div class="field">
    <strong>Que hacemos</strong><br>
    <%= f.text_area :we_do %>
  </div>
  <div class="actions">
    <%= f.submit 'Guardar' %>
  </div>
<% end %>
