<!DOCTYPE html>
<html>
<head>
	<title>Siteapp</title>
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  	<%= csrf_meta_tags %>
	<% if current_user %>
		<table>
			<tr>
				<td>LOGO</td>
				<td><%= link_to 'DASHBOARD', companies_path %></td>
				<td>
					<select name="perfil">
						<option>Hola <strong><%= current_user.name %> (<%=current_user.email%>)</strong>.</option>
						<option>Editar Perfil</option>
						<option><%= link_to 'Logout', destroy_user_session_path, method: :delete %></option>
					</select>
					<option><%= link_to 'Logout', destroy_user_session_path, method: :delete %></option>
				</td>
			</tr>
		</table>
	<% end %>
</head>
<body>



<%= yield %>

</body>
</html>
