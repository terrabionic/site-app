<div class="layout">

  <%= render 'shared/nav' %>  

  <div class="main">
      <div class="form-container">
        <h1 class="main-headline">Dar de alta nuevo consultor</h1>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>

          <h4 class="title-line">Roles</h4>
         
          <div>
            <% User::Role.all.each do |role_id| %>
                <% unless role_id.role == 'company' %>
                  <label>
                      <%= f.radio_button(:role_id, role_id.id) %>
                      <%= role_id.name %>
                  </label>
                <% end %>
            <% end %>
          </div>

           <h4 class="title-line">Acerca del usuario</h4>

          <div class="field">
            <strong>Nombre</strong><br />
            <%= f.text_field :name, autofocus: true %>
          </div>

          <div class="field">
            <strong>Email</strong><br />
            <%= f.email_field :email%>
          </div>

          <div class="field">
            <strong>Lugar</strong><br />
            <%= f.text_field :location %>
          </div>

          <div class="field">
              <strong>Número máximo de empresas</strong><br />
              <%= f.text_field :num_max_companies%>
          </div>

          <div class="field">
            <strong>Contraseña</strong>
            <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> caracteres mínimo)</em>
            <% end %><br />
            <%= f.password_field :password, autocomplete: "off" %>
          </div>

          <div class="field">
            <strong>Confirmación de contraseña</strong><br />
            <%= f.password_field :password_confirmation, autocomplete: "off" %>
          </div>

          <div>
            <p>Será enviado un email que contiene un enlace a la dirección de correo electrónico proporcionada.El destinatario la usara para iniciar sesión y establecer su contraseña</p>
          </div>

          <div class="actions">
            <%= f.submit "Crear Usuario" %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
        
      </div>
  </div>
</div>


<%= render "shared/footer" %>