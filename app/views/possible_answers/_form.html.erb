<%= form_for [@question, @possible_answer] do |f| %>
  <% if @possible_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@possible_answer.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% @possible_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :grade %><br>
    <%= f.text_field :grade %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
