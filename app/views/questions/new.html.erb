<div class="layout">
	<%= render 'shared/nav' %>

	<div class="main">
		<div class="form-container">
			<%= link_to 'Atrás', survey_questions_path(@survey), class: "back-button" %>
			<h1 class="main-headline inline-block">Nueva Pregunta</h1>

			<%= render 'form' %>
		</div>
		
	</div>

</div>

<%= render "shared/footer" %>