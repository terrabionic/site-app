<%= form_for [@survey, @question] do |f| %>
  <% if @question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% @question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Titulo:</strong><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <strong>Factor:</strong><br>
    <%= f.collection_select(:category_id, Category.all, :id, :title) %>
  </div>
  <div class="actions">
    <%= f.submit 'Guardar'%>
  </div>
<% end %>
