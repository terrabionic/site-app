<p id="reply-message" class="hide"><%= flash[:notice] %></p>

<div class="layout">

  <div class="nav">
    <a href="#" class="nav__item">
      <%= image_tag "calificacion-empresa-tab.png" %>      
      <span>Calificación de la empresa</span>
    </a>
    
  </div><!-- .nav -->

  <div class="main">
<% if current_user.change_password %>
      Cambiar Contraseña
      <a id="thx-link" class="hide" data-open="exampleModal2">Thx</a>
<%end%>
    <div class="form-container form-container--large">

  	 <h1 class="main-headline inline-block"><%= current_user.company_name %></h1>
    
      <h2 class="color-green text-bigger mb-phi">MI PYME IDEAL</h2>

      <div class="general-chart">

        <div class="max-1000">
          <div class="medium-9 columns">
            <div id="chart-categories" data-rows="<%= current_user.company.survey_analysis.reply.score_category(categories:current_user.company.survey_analysis.reply.survey.categories) %>" style="width: 600px; height: 400px;"></div>
          </div>
          <div class="medium-3 columns general-details">
            <h5>DETALLE</h5>
        
            <h6><small>PUNTAJE OBTENIDO</small></h6>
            <% if current_user.company %>
              <% if current_user.company.survey_analysis %>
                <strong><%= current_user.company.survey_analysis.reply.score %> pts.</strong>
              <% end %>
            <% end %>
            
            <h6><small>PUNTAJE IDEAL</small></h6>

            <% if current_user.company %>
              <% if current_user.company.survey_analysis %>
                <strong><%= current_user.company.survey_analysis.reply.survey.total_score %> pts. 100%</strong>
              <% end %>
            <% end %>
          </div>
        </div>

      </div><!-- .general-chart -->      

      <h3 class="hide">Este solo es un reporte general, el consultor te hara entrega del reporte de resultados de tu empresa</h3>

    </div><!-- .form-contaienr -->

    </div><!-- .main -->
</div><!-- .layout -->

<%= render "shared/footer" %>

<div class="reveal tiny" id="exampleModal1" data-reveal>
  <div class="text-center">
    <h4 class="color-blue bold">Importante</h4>
    <br>
    <p class="color-gray bold">Aquí aparecerá información sobre el <span class="color-green">diagnóstico de tu empresa</span>, nosotros nos pondremos en contacto contigo.</p>  
  </div>

  <button class="close-button" data-close aria-label="Close modal" type="button">
    <span aria-hidden="true">&times;</span>
  </button>
</div>

<div class="reveal tiny" id="exampleModal2" data-reveal>
  <div class="text-center">
    <h4 class="color-blue bold">Establecer una nueva contraseña</h4>
    <br>
    <p class="color-gray bold">Te recomendamos establecer una nueva contraseña para tu cuenta antes de comenzar a usar SITE</p>
    <%= link_to 'Cambiar Contraseña', edit_password_path(id:current_user) %>
  </div>

  <button class="close-button" data-close aria-label="Close modal" type="button">
    <span aria-hidden="true">&times;</span>
  </button>
</div>
