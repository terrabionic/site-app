
<div class="layout">
	<%= render 'shared/nav' %>

	<div class="main">
		<div class="form-container">

			<%= link_to 'Atrás', role_path(@role), class: "back-button" %>
			<h1 class="main-headline inline-block">Rol: <%= @role.name %></h1>

			<h4 class="title-line">General</h4>
			

			<%= render 'form' %>

		</div>
		
	</div>

</div>

<%= render "shared/footer" %>

