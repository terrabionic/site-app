<p id="notice"><%= notice %></p>


<div class="layout">
	<%= render 'shared/nav' %>
	<div class="main">

		<%= link_to 'Atrás', roles_path, class: "back-button" %>
		
		<h1 class="main-headline inline-block">Rol: <%= @role.name %></h1>

		<h4 class="title-line">General</h4>

		<h4 class="color-green text-bigger">Acerca del role</h4>
		<%= link_to 'Editar', edit_role_path(@role), class: "btn" %>
		<p>
		  <strong>Nombre del rol:</strong>
		  <%= @role.name %>
		</p>

		<p>
		  <strong>Descripción:</strong>
		  <%= @role.description %>
		</p>

		<p>
		  <strong>Usuarios:</strong>
		  <%= @role.get_user_by_role(@role) %>
		</p>

		<h4 class="color-green text-bigger">Permisos</h4>

		<%= link_to 'Editar', edit_permissions_path(@role), class: "btn" %>
		<p>
		  <strong>Rol:</strong>
		  <%= @role.get_role_name(@role.role)%>
		</p>

		<h4 class="color-green text-bigger">Eliminar Rol</h4>

		<p>Esta acción eliminará permanentemente este rol de su cuenta. Cualquier usuario que estuviera asignado con este role asumirá automáticamente la función de sólo vista.</p>

		<%= link_to 'Eliminar', @role, method: :delete, data: { confirm: '¿Deseas eliminar este role?
		' }, class: "btn--red" %>
		
	</div>
</div>

<%= render "shared/footer" %>

