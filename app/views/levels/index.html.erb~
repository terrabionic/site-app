<p id="notice"><%= notice %></p>

<div class="layout">

  <%= render 'shared/nav' %>

  <div class="main">
	<%= link_to 'Atrás', companies_path, class: "back-button" %>
	<h1 class="main-headline inline-block">Niveles de Curso</h1>
	<p>
		<%= link_to 'Nuevo nivel', new_level_path, class: "btn" %>	
	</p>
	
	<table class="site-table">
	  <thead>
		<tr>
		  <th>Imagen</th>
		  <th>Nombre</th>
		  <th colspan="3"></th>
		</tr>
	  </thead>

	  <tbody>
		<% @levels.each do |level| %>
		  <tr>
			<td><%= image_tag (level.image.url(:thumb)) %></td>
			<td data-title="Nombre"><%= link_to level.title, edit_level_path(level) %></td>
			<td><%= link_to 'Eliminar', level, method: :delete, data: { confirm: 'Seguro que desea eliminarlo?' } %></td>
		  </tr>
		<% end %>
	  </tbody>
	</table>

  </div><!-- .main -->

</div><!-- .layout -->

<%= render "shared/footer" %>

